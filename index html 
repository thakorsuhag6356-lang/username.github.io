Jaiho91
CasinoRank
Player rating
0reviews

Complaint response
The Platform has paid a deposit of 10 million US dollars to the ProSafeBet to ensure the safety of your bets and funds.
NOMINATE
What we like
Safety Guarantee
Download App
Join Our Telegram Channel
Jaiho91
Click to collect the Bonus now

Website

Affiliate Program

Languages

Company

Established

Licences
   


     

DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no" />
<title>JAIHO91 | Win Bigtitle>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Noto+Sans+SC:wght@300;400;500&display=swap" rel="stylesheet">
<style>
:root{
  --gold:#ffd700;
  --blue:#00f5ff;
  --purple:#bf00ff;
}
*{margin:0;padding:0;box-sizing:border-box}
body{
  background:#050810;
  font-family:'Noto Sans SC',sans-serif;
  color:#fff;
  overflow-x:hidden;
}
.grad-text{
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  background-clip:text;
  background-size:200% auto;
  animation:gradShift 5s linear infinite;
}
@keyframes gradShift{
  0%{background-position:0% center}
  100%{background-position:200% center}
}
body:before{
  content:'';
  position:fixed;inset:0;
  background:radial-gradient(circle at 20% 20%,rgba(255,215,0,.08),transparent 40%),
              radial-gradient(circle at 80% 70%,rgba(191,0,255,.08),transparent 40%);
  z-index:-1;
}
nav{
  position:fixed;left:0;right:0;top:0;
  padding:20px 60px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  background:rgba(5,8,16,.82);
  backdrop-filter:blur(16px);
  z-index:999;
  border-bottom:1px solid rgba(255,215,0,.08);
}
.logo{
  font-family:Orbitron;
  font-weight:900;
  font-size:30px;
  background:linear-gradient(90deg,var(--gold),#fff,var(--purple));
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}
nav ul{display:flex;gap:40px;list-style:none;}
nav ul a{
  text-decoration:none;
  font-size:0.82rem;
  letter-spacing:0.12em;
  font-weight:700;
  text-transform:uppercase;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  background-clip:text;
  background-size:200% auto;
  animation:gradShift 4s linear infinite;
}
nav ul a:hover{opacity:.7}
.play-btn{
  padding:12px 32px;
  background:linear-gradient(90deg,var(--gold),#ff9100);
  color:#111;
  font-weight:800;
  border-radius:50px;
  text-decoration:none;
  font-size:0.82rem;
  letter-spacing:0.1em;
  transition:transform .2s,box-shadow .3s;
}
.play-btn:hover{
  transform:translateY(-2px);
  box-shadow:0 8px 28px rgba(255,215,0,.4);
}
.hero{
  min-height:100vh;
  padding:140px 70px 80px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  flex-wrap:wrap;
  gap:50px;
}
.hero-left{max-width:640px;}
h1{
  font-family:Orbitron;
  font-size:76px;
  line-height:1.05;
  margin-bottom:20px;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  background-clip:text;
  background-size:200% auto;
  animation:gradShift 5s linear infinite;
}
.hero p{
  font-size:20px;
  line-height:1.8;
  margin-bottom:40px;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 60%,var(--purple) 100%);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  background-clip:text;
  background-size:200% auto;
  animation:gradShift 6s linear infinite;
}
.hero-buttons{display:flex;gap:20px;flex-wrap:wrap;}
.btn-main{
  padding:18px 40px;
  border-radius:50px;
  background:linear-gradient(90deg,var(--gold),#ff9300);
  text-decoration:none;
  font-weight:800;
  color:#111;
  transition:transform .2s,box-shadow .3s;
}
.btn-main:hover{transform:translateY(-2px);box-shadow:0 8px 28px rgba(255,215,0,.4);}
.btn-ghost{
  padding:18px 34px;
  border-radius:50px;
  border:1px solid rgba(255,215,0,.4);
  text-decoration:none;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  background-clip:text;
  background-size:200% auto;
  animation:gradShift 4s linear infinite;
}
.hero-right{flex:1;display:flex;justify-content:center;}
.phone-wrap{position:relative;}
.phone-ring{
  position:absolute;inset:-60px;
  border-radius:50%;
  border:1px solid rgba(255,215,0,.12);
  animation:ringPulse 4s infinite;
}
.phone-ring.r2{inset:-100px;animation-delay:1s;}
.phone-ring.r3{inset:-140px;animation-delay:2s;}
@keyframes ringPulse{50%{transform:scale(1.08);opacity:.4}}
.phone{
  width:310px;height:620px;
  border-radius:42px;padding:10px;
  background:linear-gradient(145deg,#111,#222,#000);
  box-shadow:0 0 70px rgba(255,215,0,.2),0 0 140px rgba(191,0,255,.12);
  animation:float 6s ease-in-out infinite;
}
@keyframes float{50%{transform:translateY(-20px)}}
.screen{
  height:100%;border-radius:34px;
  overflow:hidden;position:relative;background:#000;
}
.screen video{
  position:absolute;inset:0;
  width:100%;height:100%;object-fit:cover;
  pointer-events:none;
}
.screen:after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(to top,rgba(0,0,0,.35),transparent 40%),
             linear-gradient(to bottom,rgba(0,0,0,.2),transparent 22%);
  pointer-events:none;z-index:2;
}
.notch{
  position:absolute;
  width:90px;height:22px;background:#000;
  left:50%;transform:translateX(-50%);
  z-index:10;border-radius:0 0 18px 18px;
}
.stats{
  display:grid;grid-template-columns:repeat(4,1fr);
  padding:50px 40px;text-align:center;
  border-top:1px solid rgba(255,215,0,.08);
  border-bottom:1px solid rgba(255,215,0,.08);
}
.stat b{
  display:block;font-size:50px;
  font-family:Orbitron;
  background:linear-gradient(90deg,var(--gold),var(--blue),var(--purple));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;
  animation:gradShift 5s linear infinite;
}
.stat span{
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;
  animation:gradShift 6s linear infinite;
  font-size:0.82rem;letter-spacing:0.1em;text-transform:uppercase;
}
section{padding:110px 60px;}
.section-title{
  font-family:Orbitron;
  margin-bottom:60px;
  font-size:clamp(2rem,4vw,3.5rem);
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;
  animation:gradShift 5s linear infinite;
}
.section-tag{
  font-family:Orbitron;font-size:0.65rem;
  letter-spacing:0.3em;text-transform:uppercase;
  margin-bottom:16px;display:flex;align-items:center;gap:12px;
  background:linear-gradient(90deg,var(--gold),var(--blue));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;
}
.section-tag::before{content:'';display:inline-block;width:30px;height:1px;background:var(--gold);}
.features{
  display:grid;grid-template-columns:repeat(3,1fr);
  gap:2px;background:rgba(255,215,0,.06);
}
.card{
  background:#0a101a;padding:50px 35px;
  position:relative;overflow:hidden;
  transition:background .4s;
}
.card:hover{background:rgba(255,215,0,.04);}
.card::after{
  content:'';position:absolute;bottom:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,transparent,rgba(255,215,0,.35),transparent);
  transform:scaleX(0);transition:transform .4s;
}
.card:hover::after{transform:scaleX(1);}
.card h3{
  margin-bottom:18px;font-family:Orbitron;font-size:0.95rem;letter-spacing:0.06em;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 4s linear infinite;
}
.card p{
  line-height:1.8;font-size:0.88rem;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 60%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 6s linear infinite;
}
.card-num{
  position:absolute;top:20px;right:28px;
  font-family:Orbitron;font-size:3rem;font-weight:900;
  color:rgba(255,215,0,.05);line-height:1;
}
.carousel-section{padding:80px 0 120px;overflow:hidden;}
.carousel-header{padding:0 60px;margin-bottom:50px;}
.track{
  display:flex;gap:24px;
  padding:10px 60px;
  width:max-content;
  animation:scroll 28s linear infinite;
}
.track:hover{animation-play-state:paused;}
@keyframes scroll{100%{transform:translateX(-50%)}}
.shot{
  width:240px;height:430px;
  border-radius:30px;overflow:hidden;
  border:1px solid rgba(255,215,0,.25);
  position:relative;background:#111;
  box-shadow:0 12px 50px rgba(0,0,0,.4);
  transition:transform .3s,box-shadow .3s;
  flex-shrink:0;
}
.shot:hover{transform:translateY(-8px) scale(1.02);box-shadow:0 20px 60px rgba(255,215,0,.2);}
.shot img{
  width:100%;height:100%;
  object-fit:contain;    /* 完整显示图片，不裁剪 */
  background:#050810;
  display:block;
}
.shot-fallback{
  width:100%;height:100%;display:flex;align-items:center;justify-content:center;
  position:relative;
}
.shot-fallback-bg-1{background:linear-gradient(160deg,#05200f,#050810,#1a0a00);}
.shot-fallback-bg-2{background:linear-gradient(160deg,#2d1500,#050810,#1a0535);}
.shot-fallback-bg-3{background:linear-gradient(160deg,#1a0a00,#05081a,#1a0535);}
.shot-fallback-bg-4{background:linear-gradient(160deg,#1a0535,#05081a,#1a0a00);}
.shot-fallback-bg-5{background:linear-gradient(160deg,#1a1000,#050810,#05200f);}
.shot-grid{
  position:absolute;inset:0;
  background-image:linear-gradient(rgba(255,215,0,.05) 1px,transparent 1px),
                    linear-gradient(90deg,rgba(255,215,0,.05) 1px,transparent 1px);
  background-size:20px 20px;
}
.shot-label{
  position:absolute;left:0;right:0;bottom:0;
  padding:20px 16px 16px;text-align:center;
  font-family:Orbitron;letter-spacing:3px;font-size:0.7rem;
  background:linear-gradient(to top,rgba(0,0,0,.92),transparent);
  background:linear-gradient(90deg,var(--gold),var(--blue),var(--purple));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;
}
.cta{text-align:center;}
.cta h2{
  font-family:Orbitron;
  font-size:clamp(2.5rem,5vw,4.2rem);
  margin-bottom:20px;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 5s linear infinite;
}
.cta p{
  font-size:1rem;margin-bottom:14px;letter-spacing:0.05em;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 6s linear infinite;
}
.downloads{
  margin-top:45px;display:flex;
  justify-content:center;gap:25px;flex-wrap:wrap;
}
.dl-btn{
  padding:20px 44px;text-decoration:none;color:#fff;
  border:1px solid rgba(255,215,0,.3);border-radius:20px;
  display:flex;align-items:center;gap:14px;
  position:relative;overflow:hidden;
  background:rgba(255,215,0,.04);backdrop-filter:blur(10px);
  transition:all .35s;
}
.dl-btn:hover{
  border-color:rgba(255,215,0,.6);
  box-shadow:0 0 30px rgba(255,215,0,.2);
  transform:translateY(-3px);
}
.dl-btn-label{text-align:left;}
.dl-btn-sub{
  font-size:0.62rem;letter-spacing:0.14em;text-transform:uppercase;
  display:block;margin-bottom:4px;
  background:linear-gradient(90deg,var(--gold),var(--blue));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}
.dl-btn-main{
  font-family:Orbitron;font-size:0.95rem;font-weight:700;display:block;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 4s linear infinite;
}
footer{
  padding:40px;text-align:center;
  border-top:1px solid rgba(255,215,0,.08);
}
footer p{
  font-size:0.75rem;letter-spacing:0.06em;
  background:linear-gradient(90deg,var(--gold) 0%,var(--blue) 50%,var(--purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 6s linear infinite;
}
.footer-links{
  display:flex;justify-content:center;gap:30px;list-style:none;margin-bottom:16px;flex-wrap:wrap;
}
.footer-links a{
  text-decoration:none;font-size:0.75rem;letter-spacing:0.08em;
  background:linear-gradient(90deg,var(--gold),var(--blue),var(--purple));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;background-size:200% auto;animation:gradShift 5s linear infinite;
  transition:opacity .2s;
}
.footer-links a:hover{opacity:.65;}
@media(max-width:980px){
  nav ul{display:none}
  h1{font-size:54px}
  .hero{padding:120px 30px 70px;justify-content:center;}
  .cta h2{font-size:42px}
  .features{grid-template-columns:1fr}
  .stats{grid-template-columns:repeat(2,1fr);gap:30px}
  section{padding:90px 30px}
  nav{padding:16px 24px}
  .carousel-header{padding:0 30px}
}
style>
head>
<body>

<nav>
  <div class="logo">JAIHO91div>
  <ul>
    <li><a href="#features">Featuresa>li>
    <li><a href="#games">Gamesa>li>
    <li><a href="#download">Downloada>li>
  ul>
  <a class="play-btn" href="https://91jaiho.vip/?code=VZL5F6LSJ15" target="_blank" rel="noopener">PLAY NOWa>
nav>

<section class="hero">
  <div class="hero-left">
    <h1>Win Big<br>With JAIHO91h1>
    <p>India's most thrilling real-money gaming platform.<br>Massive jackpots, lightning-fast gameplay and millions of winners.p>
    <div class="hero-buttons">
      <a href="https://91jaiho.vip/?code=VZL5F6LSJ15" target="_blank" rel="noopener" class="btn-main">Play Freea>
      <a href="#games" class="btn-ghost">Explore Gamesa>
    div>
  div>
  <div class="hero-right">
    <div class="phone-wrap">
      <div class="phone-ring">div>
      <div class="phone-ring r2">div>
      <div class="phone-ring r3">div>
      <div class="phone">
        <div class="screen">
          <div class="notch">div>
          <video autoplay muted loop playsinline preload="auto">
            <source src="video_2026-04-24_23-40-07.mp4" type="video/mp4">
          video>
        div>
      div>
    div>
  div>
section>